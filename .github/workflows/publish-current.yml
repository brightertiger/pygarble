name: Publish Current Version to PyPI

on:
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        python-version: ['3.10', '3.11', '3.12']

    steps:
    - uses: actions/checkout@v4

    - name: Set up Python ${{ matrix.python-version }}
      uses: actions/setup-python@v4
      with:
        python-version: ${{ matrix.python-version }}

    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        # Install numpy<2.0 FIRST - FastText is incompatible with NumPy 2.0
        pip install "numpy>=1.21.0,<2.0"
        # Install fasttext and other deps (numpy already pinned)
        pip install fasttext-wheel pyspellchecker
        # Install dev dependencies
        pip install pytest pytest-cov black flake8 isort mypy

    - name: Run tests
      run: |
        pytest tests/ -v --tb=short

  publish:
    runs-on: ubuntu-latest
    needs: [test]

    steps:
    - uses: actions/checkout@v4

    - name: Set up Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.12'

    - name: Install build dependencies
      run: |
        python -m pip install --upgrade pip
        pip install build twine

    - name: Show current version
      run: |
        grep "version" pyproject.toml
        grep "__version__" pygarble/__init__.py

    - name: Build package
      run: |
        python -m build

    - name: Check package
      run: |
        twine check dist/*
        ls -la dist/

    - name: Publish to PyPI
      env:
        TWINE_USERNAME: __token__
        TWINE_PASSWORD: ${{ secrets.PYPI_API_TOKEN }}
      run: |
        twine upload dist/*
